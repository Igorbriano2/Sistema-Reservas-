# URL da API do backend
VITE_API_URL=http://localhost:3000

# Chave PUBLICAVEL da Stripe (pk_..., nao e secreta - pode ficar exposta no bundle do
# frontend). Usada pelo Stripe Elements no assistente de assinatura (/assinar).
VITE_STRIPE_PUBLISHABLE_KEY=
